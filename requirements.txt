# The dependency list now lives in pyproject.toml, so there is one source of
# truth. This file installs the project itself in editable mode, which pulls
# those dependencies in - keeping `pip install -r requirements.txt` working.
#
#   py -m pip install -r requirements.txt
#
# Optional backends:
#   pip install -e .[transformers]   # --backend transformers
#   pip install -e .[vad]            # --vad on with that backend
-e .
